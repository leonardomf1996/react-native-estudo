import React, { Component } from 'react';
import Routes from './routes';
import './config/StatusBarConfig'; // Não foi dado um nome pois não iremos ficar utilizando, apenas uma vez


const App = () => <Routes />;

export default App;
import { StatusBar } from 'react-native';

StatusBar.setBackgroundColor('#DA552F'); // setBackgroundColor() funciona apenas para android
StatusBar.setBarStyle('light-content'); // setBarStyle() funciona apenas em ios
